name: temporary RIMS full MCMC disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Full MCMC workflow disabled during one-point diagnostic."
